{
  "name": "doomscroller-backend",
  "version": "1.0.0",
  "description": "Doomscroller backend API - serves CS job market sentiment data",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "build": "bun build ./src/index.ts --compile --outfile server"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
